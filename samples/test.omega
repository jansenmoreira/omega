𐐷 use u32 as int;

declare φ (u64 -> u64, u64);

define main (void -> err : int)
{
    var n, it := φ(௧௨௩);
    err = 0;

    return;
}

type ABC
{
    a : int,
    b : char,
    c : (void -> int)
};

define φ (x : u64 -> y : (u64 -> u64), i : u64)
{

    var a : <u64, u64> = 0, 1;

    while (a[0] < x)
    {
        y, i = a[0], i + 1;
        a = a[1], a[0] + a[1];
    }
}
